You are an expert React Native developer building a high-speed hackathon MVP for a university carpooling app.

Immediate Goal: Scaffold the frontend UI using mock data based strictly on a vertical feed design. Do not build a backend yet. Do not set up Redux. Use React Navigation (Bottom Tabs) and standard React state.

CRITICAL PLATFORM RULE: We are building for ANDROID ONLY. Do not write, configure, or suggest any iOS-specific code, CocoaPods, or Info.plist configurations. Ensure all UI components and touch targets function perfectly on Android.

CRITICAL RULE FOR MAPS: Do NOT install @rnmapbox/maps or any heavy native map SDKs. We are exclusively using Mapbox REST APIs to avoid build errors.

Theme & Styling:

App-wide Dark Mode (deep charcoal/black backgrounds).

Primary accent color: Deep Purple/Indigo (e.g., #6B21A8 or similar).

Text must be crisp, white/light gray.

Step 1: The Navigation Structure
Create a Bottom Tab Navigator with the following tabs: Find (Search icon), My Rides (Car icon), Offer (A prominent, floating circular '+' button in the center), Bookings (User icon), and Analytics (Chart icon).

Step 2: The Vertical Ride Feed (Find Screen)
Create a FeedScreen displaying available rides using dummy JSON data. Use a standard FlatList with vertical scrolling.

Step 3: The Ride Card Component
Design a highly detailed, premium card component for the FlatList items. It must contain the following sections from top to bottom:

Header Row: Provider Avatar (circle), Provider Name text, an inline "verified" checkmark icon, and a prominent badge on the far right displaying the Fare (e.g., "PKR 250" with a purple background).

Route Text: * A row for "FROM" with a small green dot indicator and the Origin text (e.g., "FAST National University").

A dotted vertical line connecting to the next row.

A row for "TO" with a small red dot indicator and the Destination text (e.g., "DHA Phase 8").

Map Preview: A StaticRouteMap component (instructions below) rendered as a rounded rectangle.

Details Grid: Two columns displaying:

Calendar icon + Date/Time

User icon + "Seats: 4"

Transport type icon + "Transport: Car"

Action Row: Two buttons at the bottom. A secondary outlined button (e.g., "View Stops") and a primary, solid purple button ("Book").

Step 4: The Static Map PNG Generator
Create the StaticRouteMap component for the map preview inside the card.

It takes origin_coords and destination_coords.

Fetch the route geometry from Mapbox Directions API: https://api.mapbox.com/directions/v5/mapbox/driving/{origin};{destination}?geometries=polyline&access_token={YOUR_TOKEN}

Extract the geometry (encoded polyline).

Render a standard React Native <Image> component where the uri points to the Mapbox Static Images API: https://api.mapbox.com/styles/v1/mapbox/dark-v11/static/path-5+6B21A8-0.5({encoded_polyline})/auto/400x150?access_token={YOUR_TOKEN}. (Note the dark-v11 style and purple path to match the theme).

Execution:
Scaffold the Expo/React Native project for Android. Create the Bottom Tabs and the Find screen. Build the Card component exactly as described using placeholder Mapbox API keys and dummy JSON data. Output the code so I can run it on my Android emulator immediately.